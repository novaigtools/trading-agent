name: Crypto Trading Bot (manual fallback — runs the local rule engine)

# The trading scan runs on the laptop via Windows Task Scheduler.
# This workflow is kept only as a manual fallback (Actions tab → Run workflow).
#
# It runs BRAIN_MODE=rules: the deterministic rule engine in local_brain.py. No LLM,
# no API key, no cost. The Claude Code CLI is not installed on GitHub runners, so
# hybrid/cli modes would fall back to rules here anyway — we just say so explicitly.
on:
  workflow_dispatch:

concurrency:
  group: bot-state
  cancel-in-progress: false

jobs:
  trade:
    runs-on: ubuntu-latest
    permissions:
      contents: write          # Needed to commit state files back

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4.2.2

      - name: Set up Python 3.11
        uses: actions/setup-python@v5.4.0
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run trading bot scan (local rule engine, zero LLM calls)
        env:
          # No CLAUDE_API_KEY on purpose: the paid API is what drained the credit
          # balance and left the bot brain-dead. The rule engine needs no key.
          BINANCE_API_KEY: ${{ secrets.BINANCE_API_KEY }}
          BINANCE_SECRET_KEY: ${{ secrets.BINANCE_SECRET_KEY }}
          PAPER_TRADING: ${{ secrets.PAPER_TRADING }}
          STARTING_BALANCE: '500'
          GMAIL_SENDER: ${{ secrets.GMAIL_SENDER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          NOTIFY_EMAIL: ${{ secrets.NOTIFY_EMAIL }}
          SCAN_INTERVAL_MINUTES: '30'
          BRAIN_MODE: rules
        run: python run_once.py

      - name: Save state back to repo
        run: |
          git config user.name "Trading Bot"
          git config user.email "bot@trading-agent.com"
          git add risk_state.json trades.csv
          if git diff --staged --quiet; then
            echo "No state changes."
            exit 0
          fi
          git commit -m "Bot state update $(date -u '+%Y-%m-%d %H:%M UTC')"
          for i in 1 2 3; do
            git push && exit 0
            echo "Push failed, rebasing and retrying ($i/3)..."
            git pull --rebase
          done
          git push
